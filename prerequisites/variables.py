# Variables are dynamically typed -- no need to specify types
# types are determined at runtime
n = 0
print("n =", n)

# Multiple assignments
n, m = 0, "abc"
print("n = ", n, "m=", m)

# Increment
n = n + 1
n += 1

# None is null (absense of a value)
n = 4
n = None
print(n)
